<form method="post" action="{{ path('app_tournament_back_delete', {'id': tournament.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet élément ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tournament.id) }}">
    <button class="btn btn-danger mt-2">Supprimer</button>
</form>
